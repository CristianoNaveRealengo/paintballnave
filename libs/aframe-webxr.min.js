// A-Frame WebXR Stub
// O A-Frame 1.7.0 já inclui suporte nativo para WebXR
// Este arquivo serve como placeholder para manter compatibilidade
console.log('A-Frame WebXR: Usando suporte nativo do A-Frame 1.7.0');

// Registra componente vazio se necessário para compatibilidade
if (typeof AFRAME !== 'undefined') {
  // Componente stub para manter compatibilidade
  AFRAME.registerComponent('webxr-stub', {
    init: function () {
      console.log('WebXR stub component initialized');
    }
  });
}
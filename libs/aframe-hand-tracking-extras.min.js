// A-Frame Hand Tracking Extras Stub
// Arquivo stub para manter compatibilidade
console.log('A-Frame Hand Tracking Extras: Stub carregado');

// Registra componentes extras se necessário
if (typeof AFRAME !== 'undefined') {
  // Componente stub para hand tracking extras
  AFRAME.registerComponent('hand-tracking-extras-stub', {
    init: function () {
      console.log('Hand tracking extras stub component initialized');
    }
  });
  
  // Registra componentes de gestos básicos
  AFRAME.registerComponent('gesture-detector', {
    init: function () {
      console.log('Gesture detector stub initialized');
    }
  });
  
  AFRAME.registerComponent('pinch-detector', {
    init: function () {
      console.log('Pinch detector stub initialized');
    }
  });
}
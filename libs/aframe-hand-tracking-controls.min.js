// A-Frame Hand Tracking Controls Stub
// Arquivo stub para manter compatibilidade
console.log('A-Frame Hand Tracking Controls: Stub carregado');

// Registra componentes básicos se necessário
if (typeof AFRAME !== 'undefined') {
  // Componente stub para hand tracking controls
  AFRAME.registerComponent('hand-tracking-controls-stub', {
    init: function () {
      console.log('Hand tracking controls stub component initialized');
    }
  });
  
  // Registra sistema básico de hand tracking se não existir
  if (!AFRAME.systems['hand-tracking']) {
    AFRAME.registerSystem('hand-tracking', {
      init: function () {
        console.log('Hand tracking system stub initialized');
      }
    });
  }
}